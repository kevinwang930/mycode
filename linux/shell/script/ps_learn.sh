# ps short for process status

# -A -e select all or every processes
# -C commandlist select by command name
# -p pidlist select by pid
## -pp pidlist select by parent process ID
# output control
	# -f -F full format and extra full fomat
	# -l long format

ps 
