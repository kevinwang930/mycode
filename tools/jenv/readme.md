# config
```
echo 'export PATH="$HOME/.jenv/bin:$PATH"' >> ~/.zshrc
echo 'eval "$(jenv init -)"' >> ~/.zshrc
exec $SHELL -l
```

# command
```
jenv versions [--verbose] list current configured versions
jenv add <java_path> add jdk
jenv local
jenv global

```


